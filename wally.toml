[package]
name = "asyrawih/unrest-core-system"
version = "0.1.0"
realm = "shared"
registry = "https://github.com/UpliftGames/wally-index"

[dependencies]
# Transport library for the net layer. NOT a framework dependency: the framework core
# must never require it. It is used by the user-owned transport adapter that plugs into
# the Transport interface described in docs/PROPOSAL-TRANSPORT.md.
ByteNet = "elitriare/bytenet-max@1.0.0"
Scythe = "synttx/scythe@1.2.0"

[dev-dependencies]
